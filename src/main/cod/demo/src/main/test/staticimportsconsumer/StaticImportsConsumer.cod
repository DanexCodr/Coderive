unit test.staticimportsconsumer

use {
    test.StaticImportsMethodProvider.ping(*),
    test.StaticImportsMethodProvider.add(int,int),
    test.StaticImportsFieldProvider.PI,
    test.StaticImportsFieldProvider.**
}

share main() {
    ping()
    add(3, 4)
    out(PI)
    scale()
}

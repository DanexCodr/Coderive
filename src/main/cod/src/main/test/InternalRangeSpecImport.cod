unit test

use {internal.RangeSpec, internal.MultiRangeSpec}

share InternalRangeSpecImport {
    share main() {
        simple := internal.RangeSpec(none, 1, 5)
        stepped := internal.RangeSpec(2, 1, 9)
        specs := internal.MultiRangeSpec([simple, stepped])

        out(simple.toText())
        out(stepped.toText())
        out(specs.toText())
    }
}

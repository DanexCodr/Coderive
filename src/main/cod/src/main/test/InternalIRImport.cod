unit test

use {internal.IntMath}

share InternalIRImport {
    share main() {
        out(IntMath.abs(-42))
        out(IntMath.max(3, 9))
        out(IntMath.min(3, 9))
        out(IntMath.clamp(120, 0, 100))
        out(IntMath.clamp(25, 0, 100))
    }
}
